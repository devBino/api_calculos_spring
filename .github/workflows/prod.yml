name: Deploy Microsserviços Calculos

on:
    push:
        branches: [develop]

jobs:
    build:
        runs-on: ubuntu-22.04
        steps:
            - name: Checkout code
              uses: actions/checkout@v3

            - name: Setupe Java
              uses: actions/setup-java@v3
              with:
                distribution: 'temurin'
                java-version: '17'

            - name: Acessando sub projeto Api-Rest
              run: cd api-rest
            - name: Build Project Api-Rest
              run: mvn clean install -DskipTests
            
            - name: Acessando sub projeto Calculo-Processor
              run: cd ../calculo-processor/
            - name: Build Project Calculo-Processor
              run: mvn clean install -DskipTests

            - name: Retornando para Raiz
              run: cd ..

            - name: Exibindo diretorios
              run: ls la
